# wwwd
### Overview
wwwd (World Wide Web Daemon) is a small HTTP server written in Rust.
### Building
1. Make sure you have Rust installed.
2. `cargo build` in the project directory. Alternatively you can run `cargo run` to build and run directly.
